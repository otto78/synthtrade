services:
  backend:
    build: ./synthtrade/backend
    ports:
      - "8000:8000"
    env_file:
      - ./synthtrade/backend/.env
    volumes:
      - ./synthtrade/backend:/app
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
